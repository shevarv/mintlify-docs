---
title: Калькулятор бітів
---

import React, { useState } from 'react';

const BIT_DATA = [
   {  0,  1,  'ДСФМУ',  'psanctions',  'Реєстр Санкцій і застереження' },
  {  1,  2,  'РНБО',  'psanctions',  'Реєстр Санкцій і застереження' },
  {  2,  4,  'ООН',  'psanctions',  'Реєстр Санкцій і застереження' },
  {  3,  8,  'Європейский союз',  'psanctions',  'Реєстр Санкцій і застереження' },
  {  4,  16,  'Велика Британія',  'psanctions',  'Реєстр Санкцій і застереження' },
  {  5,  32,  'США',  'psanctions',  'Реєстр Санкцій і застереження' },
  {  6,  64,  'Канада',  'psanctions',  'Реєстр Санкцій і застереження' },
  {  7,  128,  'Інші іноземні санкційні списки',  'psanctions',  'Реєстр Санкцій і застереження' },
  {  8,  256,  'Нотаріуси',  'notarius',  'Нотаріуси' },
  {  9,  512,  'Національні Політично значущі особи',  'pep',  'Реєстр політично значущих осіб' },
  {  10,  1024,  'Політично значущі особи міжнарожний організацій',  'pep',  'Реєстр політично значущих осіб' },
  {  11,  2048,  'Іноземні Політично значущі особи',  'pep',  'Реєстр політично значущих осіб' },
  {  12,  4096,  'Близькі та пов\'язані до публічних осіб',  'pep',  'Реєстр політично значущих осіб' },
  {  13,  8192,  'Зрадники і окупанти',  'zrada/okupant',  'Реєстр зрадників/ Реєстр окупантів' },
  {  14,  16384,  'Податкові нерезиденти',  'nonrezedent',  'Реєстр нерезедентів' },
  {  15,  32768,  'Втрачені/загублені національні та закордонні паспорта',  'lostdoc',  'Реєстр втрачених документів' },
  {  16,  65536,  'Виконавчі провадження',  'executive',  'Виконавчі провадження' },
  {  17,  131072,  'Боржники',  'taxdebtor/debtor',  'Реєстр боржників' },
  {  18,  262144,  'резерв',  '',  '' },
  {  19,  524288,  'Банкрути',  'bankrupt',  'Реєстр банкрутів' },
  {  20,  1048576,  'Причетність до юридичної особи',  'edr',  'Реєстр юридичних осіб та ФОП' },
  {  21,  2097152,  'резерв',  '',  '' },
  {  22,  4194304,  'Інформація з ЄДР по юридичній особі',  'edrfullinfo',  'Реєстр юридичних осіб та ФОП' },
  {  23,  8388608,  'ЄДР зміни (ОПФ/назва)',  'edrchanges',  'Реєстр юридичних осіб та ФОП' },
  {  24,  16777216,  'ЄДР зміни (адреса)',  'edrchanges',  'Реєстр юридичних осіб та ФОП' },
  {  25,  33554432,  'ЄДР зміни (керівник)',  'edrchanges',  'Реєстр юридичних осіб та ФОП' },
  {  26,  67108864,  'ЄДР зміни (КВЕД)',  'edrchanges',  'Реєстр юридичних осіб та ФОП' },
  {  27,  134217728,  'ЄДР зміни (статус)',  'edrchanges',  'Реєстр юридичних осіб та ФОП' },
  {  28,  268435456,  'ЄДР зміни (КБВ)',  'edrchanges',  'Реєстр юридичних осіб та ФОП' },
  {  29,  536870912,  'ЄДР зміни (власник)',  'edrchanges',  'Реєстр юридичних осіб та ФОП' },
  {  30,  1073741824,  'Судові справи',  'justice',  'Реєстр судових справ' },
  {  31,  2147483648,  'Ліцензії',  'license',  'Реєстр ліцензій' },
  {  32,  4294967296,  'резерв',  '',  '' },
  {  33,  8589934592,  'Особи в розшуку',  'wanted',  'Реєстр розшуку' },
  {  34,  17179869184,  'резерв',  '',  '' },
  {  35,  34359738368,  'Корупційні правопорушення',  'corrupt',  'Реєстр корупціонерів' },
  {  36,  68719476736,  'резерв',  '',  '' },
  {  37,  137438953472,  'Застереження',  'psanctions',  'Реєстр Санкцій і застереження' },
  {  38,  274877906944,  'Пов\'язані з санкційними особами',  'psanctions',  'Реєстр Санкцій і застереження' },
];



export default function BitCalculator() {
  const [decimalValue, setDecimalValue] = useState(0);
  const [selectedBits, setSelectedBits] = useState(new Set());

  // оновлюємо вибір реєстрів при заповненні інпута з готовою суммою бітів
  const handleDecimalChange = (e) => {
    const value = parseInt(e.target.value) || 0;
    setDecimalValue(value);
    
    const bits = new Set();
    BIT_DATA.forEach(bit => {
      if ((value & bit.value) === bit.value) {
        bits.add(bit.bit);
      }
    });
    setSelectedBits(bits);
  };

  // оновлюємо суму при виборі реєстру
  const handleBitChange = (bit) => {
    const newSelectedBits = new Set(selectedBits);
    if (newSelectedBits.has(bit)) {
      newSelectedBits.delete(bit);
    } else {
      newSelectedBits.add(bit);
    }
    setSelectedBits(newSelectedBits);

    // калькулятор
    let value = 0;
    newSelectedBits.forEach(b => {
      const bitData = BIT_DATA.find(item => item.bit === b);
      if (bitData) {
        value += bitData.value;
      }
    });
    setDecimalValue(value);
  };

  return (
  <>
    <div className="bit-calculator">
      <h1>Калькулятор бітів - listdata</h1>
      
      <div className="calculator-container">
        {/* Input для десяткового*/}
        <div className="input-section">
          <label htmlFor="decimal-input">Десяткове значення: </label>
          <input
            id="decimal-input"
            type="number"
            value={decimalValue}
            onChange={handleDecimalChange}
            placeholder="Введіть число"
          />
          <p className="decimal-display">Значення: {decimalValue}</p>
        </div>

        {/* Таблиця бітів */}
        <div className="table-section">
          <h2>Вибір бітів та реєстрів</h2>
          <table className="bits-table">
            <thead>
              <tr>
                <th className="checkbox-col">✓</th>
                <th>Біт</th>
                <th>Значення</th>
                <th>Опис</th>
                <th>Блоки</th>
                <th>Реєстри</th>
              </tr>
            </thead>
            <tbody>
              {BIT_DATA.map(bit => (
                <tr
                  key={bit.bit}
                  className={`bit-row ${selectedBits.has(bit.bit) ? 'selected' : 'unselected'}`}
                >
                  <td className="checkbox-cell">
                    <input
                      type="checkbox"
                      id={`bit-${bit.bit}`}
                      checked={selectedBits.has(bit.bit)}
                      onChange={() => handleBitChange(bit.bit)}
                    />
                  </td>
                  <td className="bit-col">{bit.bit}</td>
                  <td className="value-col">{bit.value}</td>
                  <td className="description-col">{bit.name}</td>
                  <td className="block-col">{bit.block ? <code>{bit.block}</code> : '—'}</td>
                  <td className="register-col">{bit.register || '—'}</td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>

        {/* обрані біти */}
        {selectedBits.size > 0 && (
          <div className="summary-section">
            <h2>Обрані Реєстри ({selectedBits.size})</h2>
            <div className="summary-content">
              <div className="summary-value">
                <span>Десяткове значення: </span>
                <strong>{decimalValue}</strong>
              </div>
              <div className="summary-list">
                {Array.from(selectedBits)
                  .sort((a, b) => a - b)
                  .map(bitIndex => {
                    const bit = BIT_DATA.find(b => b.bit === bitIndex);
                    return (
                      <div key={bitIndex} className="summary-item">
                        <span className="summary-bit">Біт {bitIndex}</span>
                        <span className="summary-name"> - {bit.name}</span>
                      </div>
                    );
                  })}
              </div>
            </div>
          </div>
        )}
      </div>
    </div>
	 </>
  );
}

<BitCalculator client:load/>
